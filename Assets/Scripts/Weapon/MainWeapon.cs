using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainWeapon : MonoBehaviour
{   
    public enum WeaponType { pistol = 1, rifle = 10, special = 100 }
    public WeaponType myWeapon;
}
